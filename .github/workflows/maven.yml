on:
  push:
    branches: [main, Back]
  pull_request:

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Docker and Docker Compose
        run: |
          sudo apt-get update
          sudo apt-get install -y apt-transport-https ca-certificates curl software-properties-common
          curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo apt-key add -
          sudo add-apt-repository "deb [arch=amd64] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable"
          sudo apt-get update
          sudo apt-get install -y docker-ce docker-ce-cli containerd.io
          DOCKER_CONFIG=${DOCKER_CONFIG:-$HOME/.docker}
          mkdir -p $DOCKER_CONFIG/cli-plugins
          curl -SL https://github.com/docker/compose/releases/download/v2.34.0/docker-compose-linux-x86_64 -o $DOCKER_CONFIG/cli-plugins/docker-compose
          chmod +x $DOCKER_CONFIG/cli-plugins/docker-compose
          sudo ln -s $DOCKER_CONFIG/cli-plugins/docker-compose /usr/local/bin/docker-compose
          docker-compose --version
      - name: Set up JDK 21
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '21'
  
      - name: Run backend tests
        run: |
          cd Quiz_Back
          mvn package
  
      - name: Teardown
        run: docker-compose down


  cd:
    needs: ci
    runs-on: ubuntu-latest
    steps:
      # Étape de Checkout
      - name: Checkout
        uses: actions/checkout@v4
  
      # Se connecter à Docker
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ingivre
          password: ${{secrets.DOCKER_EMELINE_PASSWORD}}
  
      # Construire l'image Docker
      - name: Pull images
        run: |
          docker pull ingivre/quiz_challenge_devops:backend
          docker pull ingivre/quiz_challenge_devops:mysql-8.0
          
      # Pousser l'image Docker vers le registre
      - name: Push Docker images
        run: |
          docker push ingivre/quiz_challenge_devops:backend
          docker push ingivre/quiz_challenge_devops:mysql-8.0
